{
	"require-dev": {
		"jakub-onderka/php-parallel-lint": "0.9"
	},
	"scripts": {
		"test": [
			"parallel-lint . --exclude vendor",
			"npm test"
		],
		"post-install-cmd": [
			"npm cache clean -f",
			"node --version",
			"npm --version",
			"npm install npm -g",
			"node -v"
		],
		"post-update-cmd": [
			"npm cache clean -f",
			"node --version",
			"npm --version",
			"npm install npm -g",
			"node -v"
		]
	}
}
