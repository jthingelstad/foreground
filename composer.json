{
	"require-dev": {
		"jakub-onderka/php-parallel-lint": "0.9"
	},
	"scripts": {
		"test": [
			"parallel-lint . --exclude vendor"
		],
		"post-install-cmd": [
			"npm install"
		],
		"post-update-cmd": [
			"npm install"
		]
	}
}
